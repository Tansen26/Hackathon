# Core
streamlit==1.55.0
prophet==1.3.0
pandas==2.2.3        # pin to a version that works with Prophet and doesn’t conflict

# Visualisation & math
matplotlib==3.10.8
numpy==2.2.4

# Data fetching
requests==2.33.0

# Altair (compatible with Streamlit 1.55.0)
altair==4.2.2

# Dependencies that help with stability
pystan==3.9.0         # ensures Prophet works
cmdstanpy==1.3.0
